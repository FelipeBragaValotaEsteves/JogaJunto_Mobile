import styled from 'styled-components/native';

export const BackButtonAuth = styled.TouchableOpacity`
  color: #F5F7FA;
  position: absolute;
  top: -60;
  left: 0;
  z-index: 10;
`;

export const BackButtonTab = styled.TouchableOpacity`
  color: #2B6AE3;
`;